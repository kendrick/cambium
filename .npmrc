# ADR-0002 makes exact pinning the policy. Without this, the next `pnpm add` reintroduces a caret
# range and the measured figures in docs/research/oss-landscape.md stop describing what is installed.
save-exact=true
